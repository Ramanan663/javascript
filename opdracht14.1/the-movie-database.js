const favoriteMovie = function(title, duration, stars) {
    let info = title + " lasts for " + duration + " - minuts ";
    let actuer = stars;
    console.log(info, "stars -", actuer);
};
favoriteMovie("load of the rings", 90, "xxxx,xxx,xx");